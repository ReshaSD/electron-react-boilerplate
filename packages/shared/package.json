{
  "name": "@app/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist",
  "scripts": {
    "clean": "rm -f *.tsbuildinfo || true && rm -rf dist/* || true",
    "compile": "tsc -b -f",
    "compile:watch": "tsc -b -f -w"
  },
  "dependencies": {
    "sass": "^1.34.0"
  },
  "peerDependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@types/sass": "^1",
    "typescript": "^4.2.4"
  }
}
